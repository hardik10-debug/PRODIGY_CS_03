# PRODIGY_CS_02
//must install flask before running
pip install flask
Remember to keep user.html file in a folder named "templates" in your python workspace
python pass.py  //command in terminal
Open a web browser and go to http://localhost:5000. You should see the Password Complexity Checker interface.
